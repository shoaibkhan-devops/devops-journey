# 🌍 Kubernetes Ingress

Ingress provides HTTP/HTTPS routing into Kubernetes services, usually through an ingress controller.

```text
Internet
   ↓
Ingress Controller
   ├── /api  → api-service
   └── /web  → web-service
```

Learn alongside TLS certificates, DNS, health checks and network security.
